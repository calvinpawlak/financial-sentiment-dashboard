# Superseded — see AGENTS.md

This file was written before it was confirmed how Codex actually loads
project instructions. Codex reads `AGENTS.md` from the project root
automatically — there is no "Project Instructions" field to paste text
into (that's a different ChatGPT surface, not Codex).

All content that used to live here now lives in `AGENTS.md`. This file is
kept only so old links/references don't 404; it can be deleted once
Calvin confirms nothing still points at it.
